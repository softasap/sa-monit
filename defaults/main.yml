---
# defaults file for sa-monit
  root_dir: "{{ role_path }}"
  role_dir: "{{ role_path }}"

  monit_version: "5.14-2" #  FOR 16.04 "5.19.0-1"
  monit_gui_user: admin
  monit_gui_password: xeR5hSdSQcCnFVLV8PLy

  monit_presets: ["basic"]
