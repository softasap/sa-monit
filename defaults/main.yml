---
# defaults file for sa-monit
  root_dir: "{{ role_path }}"
  role_dir: "{{ role_path }}"

  monit_version: default # "5.26.0-4" #  FOR 16.04 "5.16-2"
  monit_gui_user: admin
  monit_gui_password: xeR5hSdSQcCnFVLV8PLy

  monit_presets: ["basic"]
